import React from 'react'
import { ListGroup, Image, Card, Button } from 'react-bootstrap'
import './Category.css'
import ProductDetails from 'components/ProductDetails'

/*
 Need to build out the Category component to:
  -take in a category and it's corresponding list of products 
  -call productsList component, passing in products
 */

function Category({ product, handleShowEditModal, handleShowDelModal, loggedIn }) {

  return (
    <div>
     hello from category
    </div>
  )
}

export default Category
